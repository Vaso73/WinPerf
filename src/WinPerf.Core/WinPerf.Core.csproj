<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <PropertyGroup Condition="'$(WinPerfEdition)' == 'Free'">
    <DefineConstants>$(DefineConstants);WINPERF_FREE</DefineConstants>
  </PropertyGroup>

</Project>
